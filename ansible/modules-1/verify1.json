{
    "command": [
        "sh",
        "-c",
        "[[-x $(ansible nodes -m copy -a "src=/tmp/bulutbilisimciler.txt dest=/tmp/bulutbilisimciler.txt" | grep SUCCESS) -v ansible ]] && exit 0"
    ]
}
